# 서비스 설계 기준

> 이 서비스가 **무엇을, 왜** 제공하는가를 규정한다. "어떻게 보이는가"는 [design_architecture.md](./design_architecture.md), "어디에 구현하는가"는 [vue_architecture.md](./vue_architecture.md)가 답한다.

세 문서 역할 분담:

| 파일 | 답하는 질문 | 범위 |
| --- | --- | --- |
| **service_architecture.md** (이 문서) | 무엇을, 왜 만드는가 | 컨셉·사용자·기능·우선순위·작동 규칙·흐름·연계·데이터 구조·권한·오류·API·광고 정책 |
| [design_architecture.md](./design_architecture.md) | 어떻게 보여줄 것인가 | 정보 우선순위, 배치, 컴포넌트 시각 규격, 토큰, 상태 표현, 반응형, 접근성 |
| [vue_architecture.md](./vue_architecture.md) | 어디에 구현할 것인가 | 계층, 상태 배치, Pinia Store, 로직 소유권, 데이터 흐름, 라우팅, API 계층, 마이그레이션 |

경계 예시: "즐겨찾기는 로그인해야 저장된다"(서비스) / "즐겨찾기 별 아이콘은 24px"(디자인) / "즐겨찾기 상태는 favoriteStore가 소유한다"(Vue).

상태 표기: `[현재]`(코드에 구현·근거 file:line 필수) / `[예정]`(결정했으나 미구현) / `[결정 필요]`(미확정).

---

## 1. 서비스 컨셉

> **지금 밖에 나가도 되는지를 대신 판단해 주는 날씨 서비스.**

기온·습도·풍속·강수를 나열해서 해석을 사용자에게 맡기는 대신, 위험 여부와 권장 행동을 서비스가 먼저 판정해 제시한다. 화면의 최종 산출물은 숫자가 아니라 **결론**이다.

일반 날씨 앱은 "제주 29°C 폭우, 대구 33°C 맑음"을 나열하고 해석을 사용자에게 맡긴다. 이 서비스는 "제주: 폭우+강풍 동시 발생, 외출 삼가세요 / 대구: 기온 높음, 더위 주의"처럼 판정 결과를 낸다.

컨셉은 이미 코드로 한 번 구현되어 있다 `[현재]`:

```js
// src/components/exercise/weatherMockData.js:13
export const isDangerWeather = (item) => item.status === '폭우' || item.windSpeed >= 50 || item.temp >= 30
```

`WeatherAlertView.vue:15`가 이 함수로 경보 목록을 만들고, `WeatherBadge.vue:27-33`의 추천 문구 분기 합집합도 논리적으로 동일한 조건이다. 다만 이 판정 규칙이 **목업 데이터 파일에 얹혀 있어**, mock을 API로 교체하면 서비스의 핵심 로직이 함께 사라진다. 위치 이전은 [vue_architecture.md](./vue_architecture.md) 5.3절에서 다룬다.

### 기능 채택 기준

```text
새 기능 후보
  → 이 기능이 "나가도 되는가" 판단을 직접 만드는가?  Yes → P0 후보
  → 판단의 정확도·반복 비용을 개선하는가?           Yes → P1 후보
  → 서비스 운영에 필요한 최소 요건인가?              Yes → P2 후보
  → 위 전부 No                                    → 채택 안 함
```

> 판정에 쓰이지 않는 값은 화면에 넣지 않는다. 값을 추가하려면 그 값을 쓰는 판정 규칙을 먼저 추가한다.

**채택하지 않는 기능**(이유와 함께 기록해야 재현 가능한 기준이 된다):

| 후보 | 탈락 이유 |
| --- | --- |
| 실시간 기상 레이더 애니메이션 | 원시 관측 화면이라 해석을 사용자에게 되돌려준다 |
| 시간별 기온 그래프 탐색 UI | 그래프 자체는 판정을 만들지 않는다. 필요한 것은 예보(F-11) 판정 문구다 |
| 과거 기후 통계 조회 | 판정 시점이 현재/근미래가 아니면 범위 밖 |
| 사진 공유·댓글 커뮤니티 | 체류 시간을 늘리는 기능이지 판정 개선 기능이 아니다 |
| 기상 용어 사전 | 해석을 사용자에게 맡기지 않는다는 컨셉과 반대. 문구가 불충분하다는 신호로 읽는다 |
| 관측 원시값 전체 노출(기압·이슬점 등) | 판정에 쓰이지 않는 값을 늘리는 방향 |

`humidity`는 카드에 표시되지만(`WeatherCard.vue:20`) 판정에는 단독으로 쓰이지 않는다(`WeatherBadge.vue:31`에서만 `temp>=30`과 결합). 계속 표시할지, 판정에 쓸지는 `[결정 필요]`.

---

## 2. 사용자와 사용 목적

사용자는 직업이 아니라 **사용 상황**으로 나눈다. 같은 사람이 아침엔 출근 판단을, 주말엔 야외활동 판단을 하기 때문이다.

| ID | 유형 | 언제/어디서 | 필요한 판단 | 연결 기능 | 상태 |
| --- | --- | --- | --- | --- | --- |
| U-1 | 출퇴근 직전 확인자 | 현관 앞, 평일 아침·저녁 | 우산·겉옷·대중교통 지연 여부 | F-01 목록, F-05 위험판정, F-07 추천 | `[현재]` |
| U-2 | 야외 활동 계획자 | 실내, 일정 확정 직전 | 계획 진행/변경/시간 조정 | F-02 검색, F-04 상세, F-11 예보 | 예보 없어 절반만 지원 |
| U-3 | 위험 상황 확인자 | 재난 문자 직후 | 외출 중단/경로 변경 | F-06 경보 목록 | `[현재]` |
| U-4 | 원격지 확인자 | 임의 | 특정 도시 확인 후 연락 | F-02 검색, F-04 상세 | `[현재]` |
| U-5 | 반복 확인자 | 매일 같은 도시들 | 오늘 신경 쓸 도시가 있는가 | F-14 즐겨찾기 | `[예정]` |

**대상으로 삼지 않는 사용자**: 기상 전문가(원시 데이터 정밀 비교가 필요 — 이 서비스는 데이터를 축약한다), 업무상 법적 책임이 필요한 종사자(농업·해운 등 — 판정 문구·임계값이 화면과 어긋난 이력이 있어 신뢰 보증 수준이 부족하다), 데이터 재가공 개발자(판정 이전의 원본을 원한다), 특정 취미 종사자(종목별 전용 지표가 필요해 판정 규칙이 종목 수만큼 늘어난다).

---

## 3. 기능 목록과 우선순위

### 기능 ID 체계

기존 문서는 같은 기능을 "도시 비교"/"지역 비교"로 다르게 불렀다. ID로 고정해 이름이 바뀌어도 참조가 깨지지 않게 한다. `F-` + 두 자리 번호, 발급 순서대로 부여, 폐기해도 재사용 금지, 재배열 금지(행 지우지 않고 `[폐기]` 표기).

### 우선순위 정의

| 등급 | 판정 문장 |
| --- | --- |
| P0 | 이 기능을 빼면 컨셉 문장("판단해 주는 서비스")이 성립하지 않는다 |
| P1 | 컨셉은 성립하지만 정확도·반복 사용성이 크게 떨어진다 |
| P2 | 판정과 무관, 운영상 필요 |

> 어떤 기능의 우선순위는 그 기능이 의존하는 기능의 우선순위보다 높을 수 없다.

이 규칙이 필요한 이유: 즐겨찾기(F-14)만 보면 컨셉 기여가 없어 P2가 맞지만, F-14·F-16이 의존하는 로그인(F-15)을 P2로 두면 P1 기능이 영원히 동작할 수 없다. 그래서 F-15/F-16은 의존성 규칙으로 P1까지 끌어올린다.

### 기능 목록

| ID | 기능 | 우선순위 | 상태 | 근거 |
| --- | --- | --- | --- | --- |
| F-01 | 지역별 현재 날씨 목록 | P0 | `[현재]` | `WeatherHomeView.vue:14` |
| F-02 | 도시명 검색 | P0 | `[현재]` | `WeatherHomeView.vue:38-47` |
| F-03 | 날씨 상태 검색 | P1 | `[현재]` | `WeatherHomeView.vue:62-68` |
| F-04 | 지역 상세 날씨 | P0 | `[현재]` | `WeatherDetailView.vue` |
| F-05 | 위험 날씨 판정 | P0 | `[현재]` | `weatherMockData.js:13` — **컨셉 그 자체** |
| F-06 | 위험 날씨 경보 목록 | P0 | `[현재]` | `WeatherAlertView.vue:15` |
| F-07 | 행동 추천 문구 | P0 | `[현재]` | `WeatherBadge.vue:24-33` |
| F-08 | 위험만 보기 필터 | P1 | `[현재, 부분]` | 과제2·3에만 존재, 서비스 화면(`WeatherHomeView`)엔 없음 |
| F-09 | 온도 단위 전환(℃/℉) | P2 | `[현재, 미연결]` | store 완성, 소비처는 UnitToggler 하나뿐 — 화면 숫자는 안 바뀜 |
| F-10 | 실데이터 연동 | P1 | `[예정]` | 판정 정확도의 상한을 결정 |
| F-11 | 단기 예보 | P1 | `[예정]` | U-2 지원에 필요 |
| F-12 | 대기질 | P1 | `[예정]` | 판정 근거 추가 |
| F-13 | 지역 비교 | P2 | `[예정]` | 배치 문제일 수 있음 — design 문서 확인 후 재판단 |
| F-14 | 즐겨찾기 도시 | P1 | `[예정]` | U-5 반복 비용 절감 |
| F-15 | 로그인/로그아웃 | **P1**(의존성 상향) | `[예정]` | F-14/F-18의 선행 조건 |
| F-16 | 회원가입 | **P1** | `[예정]` | F-15의 선행 |
| F-17 | 아이디/비밀번호 찾기 | P2 | `[예정]` | — |
| F-18 | 마이페이지 | P2 | `[예정]` | — |
| F-19 | 서비스 소개 | P2 | `[현재]` | `WeatherAboutView.vue` |
| F-20 | 잘못된 경로 안내 | P1 | `[현재]` | `NotFoundView.vue` — 판정에 도달 못 한 사용자를 되돌린다 |
| F-21 | 로딩/오류 상태 표시 | P1 | `[예정]` | F-10과 동시 투입 필수 |
| F-22 | 광고 노출 | P2 | `[예정]` | 12절 |

**현재 P0 6개는 전부 동작한다** — 이 프로젝트는 이미 컨셉 문장을 만족하는 최소 서비스를 갖고 있다. 다음 우선순위는 새 기능 추가가 아니라 **F-05 판정 규칙을 4벌에서 1벌로 통합**하는 일이다(5.3절 참조).

### 선행 의존 관계

```text
[P0 판정 계층]  F-01 목록 → F-02 검색 → F-04 상세
                F-05 판정 → F-06 경보 / F-07 추천 / F-08 필터(P1)
[P1 정확도 계층] F-10 실데이터 → F-11 예보 / F-12 대기질 / F-21 로딩·오류(동시 투입)
[P1 반복 계층]   F-16 회원가입 → F-15 로그인 → F-14 즐겨찾기 / F-18(P2) / F-17(P2)
[독립]          F-09(P2) F-13(P2) F-19(P2) F-20(P1) F-22(P2)
```

> 화살표 오른쪽은 왼쪽이 완성되기 전에 착수하지 않는다. F-09가 이 규칙을 어긴 실제 사례다 — store를 먼저 만들고 소비처를 나중에 붙이기로 한 결과, store는 완성됐는데 소비처가 안 붙어 "완성된 것처럼 보이지만 동작 안 하는 기능"이 됐다.

---

## 4. 기능 작동 기준

> Store의 action 이름과 인자는 이 절의 규칙에서 도출된다. 규칙 없이 Store부터 만들면 이름이 `setX`로 뭉개진다 — `toggleUnit()`은 이름은 정확한데 변환 함수가 빠진 채로 완성됐다(`configStore.js:15-17`).

서술 형식은 모든 기능에 8항목을 고정한다: ①트리거 ②입력 ③처리순서 ④성공결과 ⑤실패/예외 ⑥URL변화 ⑦상태변화 ⑧로그인요구.

### F-02 도시/날씨 검색

| 결정 | 내용 | 이유 |
| --- | --- | --- |
| 실시간 필터링, 버튼 없음 | `[현재 유지]` | 대상이 메모리 배열(8~수십 건)이라 확정 비용이 없다 |
| URL 반영은 `replace`, 300ms 디바운스 | `[예정]` | 현재 `push`(`WeatherHomeView.vue:32`)라 타이핑마다 히스토리가 쌓인다. 주석도 "현재 큰 의미없음"이라 인정. 디바운스는 한글 IME 조합 중 글자가 URL에 박히는 것을 막는다 |
| 상태(ref)가 원본, URL은 사본 | `[예정]` | URL을 원본으로 삼으면 입력 경로가 길어진다 |
| 두 검색어는 AND 결합 | `[현재 유지]` | `WeatherHomeView.vue:43-45`. 좁히는 것이 사용자 의도 |
| 0건 문구는 WeatherList가 단독 소유 + 초기화 버튼 | `[예정]` | 현재 문구가 `WeatherList.vue:19`/`WeatherComposition.vue:120` 두 벌로 갈려 있다 |

### F-04 상세 보기 이동

- **payload는 `cityId` 문자열 하나로 규정**한다. 현재 `WeatherCard.vue:25`가 `(name, status, humidity, windSpeed, id)` 5개 위치 인자를 emit해, 수신부(`WeatherHomeView.vue:73` 등)가 앞 4개를 버리는 코드를 쓴다. `WeatherParent.vue:93`은 시그니처가 달라 `id`가 조용히 유실된다.
- 카드 전체 클릭 = 상세 이동, 카드 내부 컨트롤만 `@click.stop`. 현재는 반대(카드=선택 표시, 버튼=이동)라 카드 클릭 결과가 사용자에게 안 보인다.
- 존재하지 않는 `cityId`는 **상세 화면 안에서 안내**한다. 404로 리다이렉트하지 않는다 — `/weather/typo`는 경로 오타가 아니라 "존재하는 경로 + 존재하지 않는 자원"이다.
- 조회는 `onMounted` 1회가 아니라 `cityId` 값 변화에 반응한다. 현재(`WeatherDetailView.vue:19-24`)는 상세→상세 직접 이동 시 갱신되지 않는다(증상은 안 보이는데, 상세에서 다른 도시로 가는 링크가 아직 없기 때문).

### F-05 위험 날씨 판정

```js
export const isDangerWeather = (item) => item.status === '폭우' || item.windSpeed >= 50 || item.temp >= 30
```

| 조건 | 값 | 근거 성격 |
| --- | --- | --- |
| 강수 | `status === '폭우'` | **라벨 매칭 — 취약.** API 응답 문구가 다르면(`weather[0].description`) 즉시 깨진다 |
| 풍속 | `>= 50` | 임의값. 목업 범위(10~60)에 맞춘 것으로 보임 |
| 기온 | `>= 30` | 임의값 |

결정:
1. 판정 입력은 표시 문자열이 아니라 **코드값**으로 한다(`statusCode`, 8절 참조).
2. 임계값 3개를 상수 1곳에 모은다. 목업→실데이터 전환은 임계값 조정을 동반해야 한다.
3. 위험 등급은 **2단계(정상/위험)로 시작**한다 — 소비처(경보 필터·배지)가 boolean으로 충분하고, 데이터 8건 중 4건뿐이라 다단계를 검증할 방법이 없다.
4. **판정 함수는 1개만 존재**한다. 화면 문구는 임계값을 직접 타이핑하지 않고 같은 출처를 참조한다 — 현재 `WeatherAlertView.vue:26`은 "60m/s 이상"이라 적지만 실제 판정은 50이다(우연히 50~59 구간 도시가 폭우라 OR로 걸려 증상이 안 보일 뿐).

### F-07 추천 행동 제시

- 위험 조건 1건당 문구 1개로 정의한다. 조합 전용 문구("폭우+강풍 동시 발생!")를 만들지 않는다 — 조건이 늘수록 조합 분기가 기하급수로 늘어난다.
- 동시 노출 최대 **2개**(우선순위: 폭우 > 강풍 > 폭염 — 즉시성 기준의 임의 서열).
- `humidity`는 판정 입력이 아니라 문구 선택 입력이다(폭염 문구를 "고온다습"으로 바꿀 뿐, 위험 여부는 안 바꾼다).
- 위험 0건일 때도 "🍃 산책하기 좋은 날씨예요!" 1개는 노출한다(배지 영역 높이 유지, 판정이 돌았다는 확인).

### F-09 온도 단위 전환

현재 `configStore`(`configStore.js:4-24`)에 `unit`/`unitSymbol`/`toggleUnit`은 있지만 **변환 함수가 없다.** `WeatherCard.vue:19`는 `°C`를 하드코딩해 store를 참조하지 않는다 — 버튼을 눌러도 화면 숫자는 안 바뀐다.

| 결정 | 내용 |
| --- | --- |
| 변환 공식 | `F = C × 9/5 + 32`, 정수 반올림 |
| 영향 범위 | 홈/경보/상세 카드 기온. 배지·경보 문구는 **숫자를 제거**(단위가 섞이면 임계값이 갈라져 보인다) |
| 습도·풍속 | 전환 대상 아님 |
| 영속성 | localStorage에 저장, 새로고침 후에도 유지, 로그아웃해도 유지(계정 데이터 아님) |
| **판정 임계값은 항상 섭씨 원본 기준** | 표시 단위가 판정에 영향을 주면 같은 URL이 사람마다 다른 결과를 보인다. 화씨로 판정하면 반올림 경계가 벌어진다(`30℃=86℉`인데 86℉가 되는 원본은 29.5~30.5℃ 구간) |

### F-14 즐겨찾기 (전체 [예정])

비로그인 사용자가 별을 눌렀을 때: **로그인 유도 후 원래 자리로 복귀**(3안 중 채택). 차단(A안)은 조회형 서비스에 갑자기 벽을 만든다. 로컬 임시저장 후 병합(B안)은 병합 규칙(중복·우선순위)이 추가로 필요해지고, 이 프로젝트는 이미 같은 데이터를 여러 벌 두는 문제(목업 4벌, 위험 규칙 4벌)를 반복하고 있다 — 새 기능에서 같은 구조를 또 만들지 않는다. 복귀 후 자동으로 즐겨찾기를 추가하지는 않는다(클릭 1회 비용은 감수).

### F-15 로그인/로그아웃 (전체 [예정])

- 성공 후 복귀는 `?redirect=<경로>` 쿼리로 전달, 값은 앱 내부 경로만 허용(외부 URL 리다이렉트 방지).
- 실패 시 화면 이동하지 않고 같은 폼에 오류 표시. 자격 증명 불일치/통신 실패 두 갈래로만 구분(어느 쪽이 틀렸는지는 노출 안 함 — 계정 존재 여부 노출 방지).
- 토큰 만료는 401 응답으로 감지(타이머로 재지 않음). 만료 시 **현재 화면이 인증 필요 화면일 때만** 로그인 화면으로 보낸다 — 현재 라우트 5개 전부 비로그인으로 완전히 동작하므로, 아무 때나 튕기면 사용자가 보던 정보를 다 잃는다.
- 인증 방식·토큰 저장 위치·회원가입 절차는 `[결정 필요]` — 백엔드가 없는 상태에서 확정하면 근거 없는 규정이 된다.

### Store action 후보 (Vue 문서로의 인계)

| 기능 | action | 소속 후보 |
| --- | --- | --- |
| F-02 | `filteredCityList`(getter), `findCityById` | `weatherStore` |
| F-05 | `isDangerWeather`(순수함수) | `domain/` |
| F-07 | `getRecommendations`(순수함수) | `domain/` |
| F-09 | `toggleUnit`, `toDisplayTemp`, `restoreUnit` | `configStore` |
| F-14 | `toggleFavorite`, `isFavorite`, `fetchFavorites` | `favoriteStore` |
| F-15 | `login`, `logout`, `handleUnauthorized` | `authStore` |

---

## 5. 사용자 흐름과 기능 간 연계

### 대표 흐름

**A. 첫 방문자 → 자기 지역 확인**: `/` 진입 → 목록에서 지역 검색 → 카드 클릭 → 상세. 이탈 지점: 목록에 없는 지역(지원 도시 8개 고정, 안내 필요 `[결정 필요]`), 목록이 비어 있음(통신 실패 vs 결과 없음 구분 필요).

**B. 위험 경보 확인 → 행동 판단**: `/weather/alerts` → 카드에서 추천 문구 확인 → 상세 이동. **끊긴 지점**: 상세 화면에는 위험 표시가 전혀 없다(`WeatherDetailView.vue:32-40`). 경보에서 판단 근거를 보고 상세로 들어갔는데 상세에서 근거가 사라진다 — **위험 판정과 추천은 경보·상세 양쪽에 동일하게 나타나야 한다.**

**C. 검색으로 상세 진입**: 검색 → 상세 → 돌아가기. **끊긴 지점**: 돌아가기가 `router.push('/')`로 쿼리 없이 이동해(`WeatherDetailView.vue:45`) 직전 검색 조건이 사라진다.

**D. 즐겨찾기 중 로그인 요구** `[예정]`: 인증이 필요한 지점은 **화면이 아니라 행위**다. `/weather/:cityId`는 누구나 보되, 그 안의 "즐겨찾기 추가"만 막힌다. Guard를 라우트에만 걸면 이 흐름을 표현할 수 없다.

**E. 잘못된 주소/존재하지 않는 도시**: 주소 자체가 없으면 404, 도시가 없으면 상세 화면 내 안내(두 "없음"을 구분).

### 기능 간 연계

```text
[도시 마스터] → Weather(현재관측) → F-01 목록 / F-02 검색 / F-05 판정 → F-06 경보 / F-07 추천
             → Forecast[예정] → F-11
             → AirQuality[예정] → F-12
             (F-04 상세는 위 전부를 한 도시에 대해 모아 보여준다)
```

> 위험 판정(F-05)은 화면이 아니라 데이터 변환이다. F-06·F-07은 그 결과를 다른 방식으로 보여줄 뿐 각자 판정하지 않는다.

**같은 데이터를 여러 곳이 각자 갖는 문제가 이미 발생했다**: 도시 목록이 세 View에 각각 import(`WeatherHomeView.vue:9`, `WeatherAlertView.vue:8`, `WeatherDetailView.vue:4`), 목업 배열 4벌(값도 이미 어긋남 — `WeatherMockup.vue:8` 부산 습도 59 vs 기준 60), 위험 판정 규칙 4벌(화면 문구와도 어긋남).

> 도시 목록과 관측 데이터는 단일 출처를 가진다. 위험 판정은 하나의 함수로만 존재하며 화면은 판정하지 않는다. 판정에 쓴 임계값은 화면 문구에도 같은 출처로 공급한다.

기능 간 계약은 **이름 있는 객체 하나**로 넘긴다. 위치 인자 나열(F-04의 5개 인자 사례)은 순서가 바뀌면 조용히 깨진다.

---

## 6. 도메인 데이터 구조

이 절의 모양이 그대로 Pinia state 모양이 된다.

### 핵심 결정 3가지

**① City와 Weather를 분리한다.** 현재는 합쳐져 있다(`weatherMockData.js:2-11`). 분리 이유: 예보는 도시 1개에 관측 N개가 필요한데 합친 모델로는 표현이 부족하다. 즐겨찾기에 합친 객체를 저장하면 저장 시점 온도가 함께 얼어버린다. 화면 결합 부담은 **표시용 결합 뷰모델**로 해소한다(저장은 분리, 화면 직전에 합침).

**② status는 코드값(`statusCode`)과 표시 문구(`statusLabel`)를 함께 둔다.** 현재 판정이 `status === '폭우'` 정확 일치인데, 이는 표시용 문자열이라 API 응답 문구가 바뀌면(다국어·표기 변경) **판정이 조용히 전부 거짓이 된다.** 검색은 `statusLabel`(부분 일치)을 대상으로 한다.

**③ 저장 단위를 고정한다.** 온도는 항상 섭씨, 표시 직전에만 변환(4절 F-09 참조). 풍속은 m/s로 고정 — 단, **현재 목업 값(10~60)은 km/h 감각으로 만들어져 있어** 실제 m/s로 보면 비현실적이다. 데이터 교체 시 임계값도 함께 재조정해야 한다.

### 모델 정의(요약)

| 모델 | 핵심 필드 | 상태 |
| --- | --- | --- |
| `City` | `id`, `name`, `apiQuery`, `lat/lon` | `[현재·재정의]` |
| `Weather` | `cityId`, `temp`(℃), `humidity`(%), `windSpeed`(m/s), `statusCode`, `statusLabel`, `observedAt` | `[현재·재정의]` |
| `Forecast` / `AirQuality` | 날짜별/도시별 파생 | `[예정]` |
| `DangerAssessment` | `isDanger`, `level`, `reasons[]`(code/threshold/actual) | `[현재·파생·저장 안 함]` |
| `Recommendation` | `actionCode`, `severity` | `[현재·파생·저장 안 함]` |
| `User` / `Favorite` | 인증·즐겨찾기 | `[예정]` |

`DangerAssessment.reasons`를 배열로 두는 이유: 포항은 폭우+풍속 조건을 동시에 만족하는데, 현재는 이 조합마다 전용 문구 분기를 만들어 조건이 늘 때마다 조합이 배로 늘어난다. 근거를 배열로 두면 조건 N개에 문구 N개만 있으면 된다.

파생 값(`DangerAssessment`, `Recommendation`)을 저장하지 않는 이유: 저장하면 원본 갱신마다 파생도 갱신해야 하는데, 하나라도 놓치면 "28도인데 폭염 경보"가 뜬다. 판정 입력이 3필드뿐이라 계산 비용이 저장·동기화 비용보다 명백히 작다.

`[결정 필요]`: 도시 마스터를 상수로 둘지 API로 받을지 / `WIND`(경주의 status, 실질은 풍속인데 상태로 중복 기록됨) 대체 코드 / `level` 단계 수 / 예보 3시간→일 접기 규칙 / 대기질 등급 기준.

---

## 7. 권한 정책

> 권한표는 라우터보다 먼저 존재한다. Guard는 이 표를 코드로 옮긴 결과여야 한다.

| 기능 | 비로그인 | 로그인 |
| --- | --- | --- |
| F-01~F-08 (날씨 조회·검색·상세·경보) | 전체 열람 가능 | 동일 |
| F-14 즐겨찾기 | 별은 보이고 누를 수 있으나 저장 안 됨 → 로그인 유도 후 복귀 | 추가·삭제·조회 가능 |
| F-15 로그인 | 사용 가능 | 홈으로 되돌림 |
| F-18 마이페이지 | 렌더링 없이 로그인 화면으로 (차단) | 사용 가능 |
| F-09 단위 전환 | 사용 가능(취향, 개인정보 아님) | 동일 |

**F-01~F-08을 전부 비로그인 열람으로 두는 이유**: 산출물이 정보가 아니라 행동 권고("외출을 삼가세요")다. 위험 경보를 로그인 뒤에 두면, 지금 나가도 되는지 판단하려는 사용자가 계정을 만드는 동안 판단을 미루게 된다.

**기본은 유도, 차단은 화면 전체가 로그인 전제일 때만**: 즐겨찾기는 유도(기능 존재를 알려야 로그인 동기가 생긴다), 마이페이지는 차단(표시할 내용이 전부 사용자 데이터라 빈 껍데기를 먼저 보이는 것이 더 나쁘다).

규칙: 날씨 열람 계열은 어떤 경우에도 로그인을 요구하지 않는다 / 로그인 요구는 저장 시점에만 발생, 화면 진입 시점이 아니다 / 로그인 후 요구 직전 URL로 복귀 / 한 흐름에서 로그인 요구는 한 번만.

---

## 8. 오류와 예외 정책

> 사용자의 입력이 정상 처리됐으나 대상이 없는 것은 오류가 아니다.

| 구분 | 정의 | 다음 행동 |
| --- | --- | --- |
| 빈 상태 | 요청 성공 + 결과 0건 | 입력을 바꾼다 |
| 오류 상태 | 요청 실패 또는 대상 무효 | 다시 시도하거나 다른 화면으로 |

이 구분이 실제로 무너진 사례: 통신 실패 시(`console.error`만 하고 목록을 빈 배열로 남김) 화면은 "검색 결과와 일치하는 도시가 없습니다"를 출력한다. 네트워크 끊김이 검색 실패로 보인다.

| 유형 | 구분 | 표시 | 재시도 | 이동 |
| --- | --- | --- | --- | --- |
| 존재하지 않는 도시 | 오류 | 요청 지역을 찾지 못함 + 목록 이동 | 불가 | 상세 화면 유지 |
| 검색 결과 0건 | 빈 상태 | 일치 지역 없음 + 검색 초기화 | — | 없음 |
| API 실패 / 네트워크 단절 | 오류 | 불러오지 못함 안내 + 재시도 | 가능 | 없음 |
| 로그인 실패 | 오류 | 이메일/비밀번호 불일치(구분 안 함) | 가능 | 로그인 화면 유지 |
| 토큰 만료 | 오류 | 재발급 성공 시 무표시, 실패 시 재로그인 안내 | 재발급 1회 후 자동 재시도 | 실패 시만 로그인 화면 |
| 잘못된 URL | 오류 | 주소 없음 + 홈 이동 | 불가 | NotFound |
| 즐겨찾기 저장 실패 | 오류 | 저장 실패 안내 + 재시도, 별 아이콘 원복 | 가능 | 없음 |

**오류 메시지 원칙**: 한 문장에 (무엇이 일어났는가 + 무엇을 할 수 있는가) / 기술 용어(API, 토큰, 상태 코드) 노출 금지 / 서로 다른 원인을 "또는"으로 묶지 않는다 / 서버 메시지를 화면에 그대로 출력하지 않는다.

현재 `WeatherDetailView.vue:42`의 "해당 지역의 상세 데이터 장부가 존재하지 않습니다"는 내부 은어이고 다음 행동이 없다 — "요청하신 지역의 날씨 정보를 찾을 수 없습니다. 지역 목록에서 다시 선택해 주세요"로 교체한다.

**오류 처리를 API 연동 전에 확정하는 이유**: 오류 처리는 화면의 분기 구조를 바꾸는 작업이라, 나중에 붙이면 이미 짠 컴포넌트를 전부 다시 열어야 한다.

---

## 9. API 요구사항

백엔드가 없으므로 이 절은 서버 구현 명세가 아니라 **클라이언트가 필요로 하는 데이터 요구 목록**이다.

### 응답 공통 규격

```text
성공: { "ok": true, "data": {...}, "meta": {...} }
실패: { "ok": false, "error": { "code": "CITY_NOT_FOUND", "message": "..." } }
```

HTTP 상태 코드만으로는 8절의 분기(존재하지 않는 도시 vs 잘못된 URL, 로그인 실패 vs 토큰 만료)를 구분할 수 없어 봉투 구조를 쓴다. `error.message`는 화면에 그대로 쓰지 않는다(서버가 기술 용어를 노출할 수 있다).

### 엔드포인트 목록 `[예정]`

| 메서드 | 경로 | 용도 | 인증 |
| --- | --- | --- | --- |
| GET | `/cities` | 도시 목록·검색 | 불필요 |
| GET | `/weather/current` | 현재 날씨(복수 cityId 동시 조회) | 불필요 |
| GET | `/weather/forecast` | 예보 | 불필요 |
| GET | `/air-quality` | 대기질 | 불필요 |
| POST | `/auth/signup`, `/auth/login`, `/auth/refresh`, `/auth/logout` | 인증 | logout만 필요 |
| GET/POST/DELETE | `/favorites` | 즐겨찾기 조회/추가/삭제 | 필요 |

복수 cityId 동시 조회가 필요한 이유: 개별 호출 + `Promise.all`은 하나만 실패해도 전체가 거부된다(현재 참고자료 `WeatherHomeView.vue.afterAxios:27-31`가 이 방식). 인증 필요 엔드포인트를 즐겨찾기·로그아웃으로 한정하는 이유는 7절 권한표와 일치시키기 위해서다.

### 현재 확인된 실측 스펙 `[현재]`

OpenWeatherMap Current Weather 2.5만 실제 호출 이력이 있다(`AxiosWeather.vue:12`). `units=metric`(섭씨), `lang=kr` 고정. 예보·대기질은 어떤 API도 호출한 적 없음.

### Mock → API 교체 원칙

> 목업과 API 응답은 같은 도메인 모델로 화면에 도착해야 한다. 변환은 경계에서 한 번만 한다.

바뀌지 않아야 하는 것: 도메인 모델 필드명·타입, 도시 식별자 체계(`city_01`), 단위 규약, 위험 판정 기준값. 바뀌는 것: 데이터 획득 경로, 동기→비동기, 실패 가능성.

**비밀값**: API 키를 소스에 커밋하지 않는다. 현재 동일 키가 3개 파일에 하드코딩되어 있고 전부 git 추적 중이다 — `.env`로 옮기는 것만으로는 안전해지지 않으며(git 이력에 남음), **재발급이 필요하다.**

---

## 10. 광고 정책

기존 두 문서는 광고를 서로 다르게 취급했다(한쪽은 상세히 다루고, 한쪽은 언급 없음). 여기서 확정한다.

> 광고 영역을 둔다. P2이며, 실제 네트워크를 연동하지 않고 **Mock 슬롯으로만** 구현한다.

Mock 슬롯도 지금 배치를 정하는 이유: 광고 슬롯은 레이아웃을 바꾸는데, 나중에 끼워 넣으면 현재 4곳에 하드코딩된 `.dashboard-wrapper` 폭을 또 손대야 한다.

| 화면 | 허용 | 개수 |
| --- | --- | --- |
| 홈 / 상세 / 예보 / 즐겨찾기 목록 / 서비스 안내 | 허용 | 화면당 최대 1 |
| **위험 날씨 경보** | **금지** | 0 |
| 로그인/회원가입/마이페이지/404/오류·빈 상태 블록 내부 | 금지 | 0 |

**위험 경보 화면 금지 이유**: 이 화면은 "지금 나가도 되는지 판단하려는" 사용자만 온다. 광고는 정의상 주의를 다른 곳으로 돌리기 위해 존재하므로, 판단을 돕는 화면에 판단을 흩트리는 요소를 놓는 것은 컨셉과 정면 충돌한다.

원칙: 화면당 최대 1개 / 스크롤 없이 보이는 첫 영역에 넣지 않는다 / 핵심 정보 블록 사이에 끼지 않는다(항상 끝에) / 사용자 조작을 가로채지 않는다(전면광고·자동재생 금지) / 광고임을 명시한다 / 로딩 실패는 오류가 아니라 조용히 슬롯을 접는다.

시각 규격(치수·색·표기)은 [design_architecture.md](./design_architecture.md), 컴포넌트 구조는 [vue_architecture.md](./vue_architecture.md) 소관.

---

## 요약 — 핵심 결정

| 결정 | 이유 |
| --- | --- |
| 컨셉을 "판단해 주는 서비스"로 좁힌다 | "정보 제공"만으로는 기능 탈락 판정이 안 된다 |
| 기능에 ID를 부여한다 | 이름이 바뀌어도 문서 간 참조가 깨지지 않는다 |
| 우선순위는 컨셉 기여도 + 의존성으로 결정 | 즐겨찾기만 보면 P2인 로그인이 실제로는 P1이어야 하는 이유 |
| 위험 판정은 함수 1개, 임계값은 상수 1곳 | 현재 4벌로 흩어져 화면 문구와 이미 어긋나 있다 |
| 상세보기 이벤트 payload는 cityId 1개 | 위치 인자 5개 중 4개가 버려지고 있다 |
| 온도 단위 전환은 판정에 영향 없음(항상 섭씨 기준 판정) | 반올림 경계에서 도시가 위험/정상을 오가는 것을 막는다 |
| status는 코드값+표시문구 분리 | API 문구 변경 시 판정이 조용히 전부 거짓이 되는 것을 막는다 |
| 즐겨찾기 비로그인 시 유도(차단 아님) | 조회형 서비스에 갑자기 벽을 만들지 않는다 |
| 광고는 Mock 슬롯, 위험 경보 화면 금지 | 판단을 돕는 서비스가 판단을 방해하는 모순을 만들지 않는다 |

## [결정 필요] 목록

| 항목 | 확정 위치 |
| --- | --- |
| 위험 판정 강풍 임계값 50 vs 60(화면 문구와 불일치) | 4절, 데이터 정비 시점 |
| 습도를 판정에 쓸지 표시만 할지 | 4절 |
| 도시 마스터 상수 vs API | 9절 |
| 인증 방식·토큰 저장 위치 | 백엔드 확보 후 |
| 비로그인 즐겨찾기 로컬 저장 허용 여부 | 4절 F-14 (현재는 미허용으로 결정) |
