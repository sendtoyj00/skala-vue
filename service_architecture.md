응. **지금은 Pinia 들어가기 직전**이니까, 코드부터 더 짜기보다 **“무엇을 전역 상태로 만들 것인가”가 결정될 정도의 설계**를 먼저 끝내면 돼.

현재 네 날씨 서비스 기준으로는 아래 순서가 가장 좋음.

# Pinia 진입 전 설계 체크리스트

| 순서 | 설계                      | 여기서 결정할 것                                            | 결과물                  |
| ---- | ------------------------- | ----------------------------------------------------------- | ----------------------- |
| 1    | **서비스 컨셉 설계**      | 이 날씨 서비스가 일반 날씨앱과 무엇이 다른가                | 서비스 한 줄 정의       |
| 2    | **사용자/사용 목적 설계** | 누가, 언제, 왜 사용하는가                                   | 사용자 유형 + 사용 상황 |
| 3    | **기능 목록 설계**        | 어떤 기능을 제공할 것인가                                   | 기능 목록               |
| 4    | **기능 우선순위 설계**    | 필수/핵심/부가 기능 구분                                    | MVP 기능표              |
| 5    | **화면(View) 설계**       | 어떤 화면이 필요한가                                        | View 목록               |
| 6    | **화면 배치 설계**        | 각 화면에서 무엇을 어디에 놓을 것인가                       | 화면 구성도             |
| 7    | **기능 작동 기준 설계**   | 버튼/검색/즐겨찾기 등이 정확히 어떻게 작동하는가            | 기능별 작동 규칙        |
| 8    | **사용자 흐름 설계**      | 사용자가 기능을 어떤 순서로 이용하는가                      | User Flow               |
| 9    | **기능 간 연계 설계**     | 검색→상세→즐겨찾기→로그인 같은 연결                         | 기능 관계도             |
| 10   | **라우팅 설계**           | URL과 View의 관계                                           | Route 표                |
| 11   | **상태 설계**             | 어떤 데이터가 컴포넌트 상태이고 어떤 데이터가 공유 상태인가 | State 목록              |
| 12   | **상태의 주인 설계**      | 각각의 상태를 누가 관리하는가                               | 상태 책임표             |
| 13   | **데이터 구조 설계**      | 날씨/도시/사용자/즐겨찾기 데이터 구조                       | 데이터 모델             |
| 14   | **권한 설계**             | 로그인 여부에 따라 무엇을 허용하는가                        | 권한표                  |
| 15   | **오류/예외 설계**        | API 실패, 잘못된 URL, 검색 실패 등을 어떻게 처리하는가      | 예외 처리표             |
| 16   | **광고 설계**             | 광고 위치/조건/노출 규칙/서비스 방해 정도                   | 광고 정책               |
| 17   | **API 연계 설계**         | 어떤 기능이 서버 데이터를 필요로 하는가                     | API 요구사항            |
| 18   | **디자인 기준 설계**      | 색상보다 먼저 정보 우선순위/가독성/일관성                   | UI 디자인 원칙          |

---

# 1. 서비스 컨셉

가장 먼저.

현재처럼

> 날씨를 보여주는 서비스

는 너무 일반적임.

예를 들어:

> **현재 날씨뿐 아니라 위험 상황과 행동 판단에 필요한 정보를 빠르게 제공하는 날씨 서비스**

처럼 **서비스의 존재 이유**를 정함.

이게 나중에 기능을 평가하는 기준이 됨.

```text
새 기능 추가?
     ↓
이 기능이 서비스 목적에 필요한가?
     ↓
Yes → 추가
No → 제외
```

---

# 2. 사용자 / 사용 목적

예:

| 사용자        | 목적                |
| ------------- | ------------------- |
| 일반 사용자   | 현재 날씨 확인      |
| 출퇴근 사용자 | 외출 전 날씨 확인   |
| 여행 사용자   | 특정 지역 날씨 확인 |
| 로그인 사용자 | 관심 도시 관리      |

여기까지는 간단하게.

---

# 3. 기능 목록

현재 네가 생각한 기능을 정리.

### 핵심

- 대시보드
- 지역별 날씨
- 날씨별 지역
- 검색
- 상세 날씨
- 7일 예보
- 대기질
- 위험 날씨/추천

### 사용자

- 회원가입
- 로그인
- 로그아웃
- 아이디/비밀번호 찾기
- 마이페이지
- 즐겨찾기

### 서비스

- 서비스 안내
- 광고

---

# 4. 기능 우선순위

모든 걸 똑같이 만들면 안 됨.

```text
P0 = 반드시 필요
P1 = 핵심 기능
P2 = 부가 기능
```

예:

| 기능        | 우선순위 |
| ----------- | -------- |
| 현재 날씨   | P0       |
| 도시 검색   | P0       |
| 상세 날씨   | P0       |
| 위험 날씨   | P1       |
| 7일 예보    | P1       |
| 즐겨찾기    | P1       |
| 대기질      | P1       |
| 로그인      | P1       |
| 광고        | P2       |
| 서비스 안내 | P2       |

---

# 5. View 설계

이제 Route와 연결되는 **페이지 단위**를 결정.

현재 구조라면:

```text
views/

WeatherHomeView
WeatherDetailView
WeatherAlertView
WeatherAboutView

LoginView
SignupView
FindAccountView
MyPageView
FavoriteView

NotFoundView
```

여기까지가 **페이지 설계**.

---

# 6. 화면 배치 설계

각 View 안에 무엇이 있는지.

예:

```text
WeatherHomeView

┌──────────────────────────┐
│ Navigation               │
├──────────────────────────┤
│ 검색                      │
├──────────────────────────┤
│ 현재 선택 지역             │
├──────────────────────────┤
│ 위험 날씨                 │
├──────────────────────────┤
│ 현재 날씨                 │
├──────────────────────────┤
│ 즐겨찾기                  │
├──────────────────────────┤
│ 지역 목록                 │
├──────────────────────────┤
│ 광고                      │
└──────────────────────────┘
```

**여기까지는 디자인이 아니라 정보 배치**임.

---

# 7. 기능 작동 기준 ⭐

이게 지금 제일 중요함.

예를 들어 검색:

```text
검색어 입력
→ 검색 버튼
→ 도시 검색
→ 존재하면 선택
→ 상세 페이지 이동
→ URL에 cityId 반영
```

즐겨찾기:

```text
★ 클릭
→ 이미 등록?
   ├─ Yes → 제거
   └─ No → 추가
```

위험 날씨:

```text
날씨 데이터
→ 위험 기준 검사
→ 위험함?
   ├─ Yes → 경고 표시
   └─ No → 표시하지 않음
```

이런 **작동 규칙**을 먼저 정함.

---

# 8. 사용자 Flow

예:

### 일반 사용자

```text
홈
 ↓
도시 검색
 ↓
도시 선택
 ↓
상세 날씨
```

### 즐겨찾기

```text
상세 날씨
 ↓
즐겨찾기
 ↓
로그인 안 됨
 ↓
로그인
 ↓
즐겨찾기 저장
```

### 로그인

```text
로그인
 ↓
인증 성공
 ↓
메인 이동
 ↓
사용자 상태 유지
```

이걸 정하면 나중에 Router + Pinia 설계가 쉬워짐.

---

# 9. 기능 간 연계

이걸 꼭 해.

예:

```text
검색
 ↓
도시 선택
 ↓
상세 날씨
 ↓
즐겨찾기
 ↓
로그인
 ↓
사용자 계정에 저장
```

또:

```text
날씨 데이터
 ├→ 현재 날씨
 ├→ 위험 판단
 ├→ 예보
 └→ 대기질
```

이렇게 **같은 데이터를 여러 기능이 어떻게 사용하는지** 결정.

---

# 10. Router 설계

현재 배운 부분.

| URL                | View              | 목적        |
| ------------------ | ----------------- | ----------- |
| `/`                | WeatherHomeView   | 메인        |
| `/weather/:cityId` | WeatherDetailView | 도시 상세   |
| `/alert`           | WeatherAlertView  | 위험 날씨   |
| `/about`           | WeatherAboutView  | 서비스 안내 |
| `/login`           | LoginView         | 로그인      |
| `/mypage`          | MyPageView        | 마이페이지  |
| `/*`               | NotFoundView      | 잘못된 주소 |

그리고:

```text
/weather/:cityId
```

의 `cityId`가 왜 필요한지도 결정.

---

# 11. 상태 설계 ⭐⭐⭐

**Pinia 바로 직전의 핵심.**

먼저 모든 데이터를 적어.

```text
날씨 데이터
검색어
선택 도시
즐겨찾기
로그인 사용자
JWT
단위 ℃/℉
위험 날씨
로딩
에러
```

그리고 각각 분류.

### A. 한 컴포넌트에서만 필요

```text
검색창 입력값
```

→ 그냥 Component `ref`

### B. 부모-자식 사이

```text
선택된 도시
```

→ Props / Emits

### C. 여러 곳에서 공유

```text
℃ / ℉
로그인 상태
즐겨찾기
현재 날씨
```

→ **Pinia 후보**

이 과정을 거쳐야 Pinia가 필요한 이유가 생김.

---

# 12. 상태의 주인

이게 네가 전에 정리한:

> **상태의 주인을 명확하게 만든다.**

부분.

예:

| 상태          | 주인                    |
| ------------- | ----------------------- |
| 검색어        | SearchBar / WeatherHome |
| 현재 도시     | Weather Store           |
| 날씨 데이터   | Weather Store           |
| 즐겨찾기      | Favorite Store          |
| 로그인 사용자 | Auth Store              |
| JWT           | Auth Store              |
| ℃/℉           | Config Store            |
| 화면 로딩     | UI 또는 각 기능 Store   |

여기까지 정하면 Pinia Store 설계가 거의 나온다.

---

# 13. 데이터 구조

예:

```js
city = {
  id,
  name,
  region,
}
```

```js
weather = {
  cityId,
  temp,
  humidity,
  windSpeed,
  status,
  observedAt,
}
```

```js
user = {
  id,
  name,
  email,
}
```

이걸 정해놓으면 나중에 API가 들어와도 덜 흔들림.

---

# 14. 권한

예:

| 기능       | 비로그인 | 로그인 |
| ---------- | -------- | ------ |
| 날씨 조회  | O        | O      |
| 검색       | O        | O      |
| 상세       | O        | O      |
| 즐겨찾기   | △        | O      |
| 마이페이지 | X        | O      |
| 로그아웃   | X        | O      |

이것도 Pinia + Router Guard의 근거가 됨.

---

# 15. 오류 기준

최소한 이것만.

```text
도시 없음
API 실패
네트워크 실패
로그인 실패
인증 만료
잘못된 URL
즐겨찾기 저장 실패
```

그리고 각각:

```text
사용자에게 무엇을 보여줄 것인가?
재시도할 것인가?
어디로 이동시킬 것인가?
```

를 정함.

---

# 16. 광고 설계

Mock 광고라도 **기능보다 먼저 배치 기준**을 정함.

예:

```text
홈
 ├─ 핵심 날씨
 ├─ 위험 정보
 ├─ 광고
 └─ 부가 정보
```

원칙:

> **광고가 핵심 날씨 정보를 가리지 않는다.**

그리고:

```text
로그인 페이지 → 광고 없음
회원가입 → 광고 없음
위험 경고 → 광고보다 우선
상세 날씨 → 광고 1개
홈 → 광고 1~2개
```

정도.

---

# 17. API 설계

아직 실제 백엔드를 만들지 않아도 됨.

**필요한 데이터가 무엇인지**만 정함.

예:

```text
GET /weather/:cityId
GET /weather/forecast/:cityId
GET /air-quality/:cityId

POST /auth/login
POST /auth/signup

GET /favorites
POST /favorites
DELETE /favorites/:cityId
```

이 정도의 **요구사항**만 잡으면 됨.

---

# 18. 디자인 기준

마지막에.

지금은 색깔부터 정하는 게 아니라:

### 정보 우선순위

```text
위험 날씨
  ↓
현재 날씨
  ↓
예보
  ↓
상세 정보
  ↓
부가 기능
  ↓
광고
```

같은 걸 정하는 것.

---

# 그래서 지금 네가 해야 할 순서

현재 프로젝트에 딱 맞춰 압축하면:

```text
① 서비스 컨셉
        ↓
② 기능 목록
        ↓
③ 기능 우선순위
        ↓
④ View 목록
        ↓
⑤ View 내부 배치
        ↓
⑥ 기능별 작동 기준 ⭐
        ↓
⑦ 사용자 Flow
        ↓
⑧ 기능 간 연계
        ↓
⑨ Route 기준
        ↓
⑩ 상태 목록
        ↓
⑪ 상태의 주인 ⭐⭐⭐
        ↓
⑫ 데이터 구조
        ↓
⑬ 권한
        ↓
⑭ 오류 처리
        ↓
⑮ 광고 배치/작동 기준
        ↓
⑯ API 요구사항
        ↓
━━━━━━━━━━━━━━━━
        ↓
      Pinia
━━━━━━━━━━━━━━━━
        ↓
Store 분리
AuthStore
WeatherStore
FavoriteStore
ConfigStore
...
```

### 특히 중요한 건 3개

**① 기능 작동 기준**

> "이 기능은 정확히 어떻게 움직이는가?"

**② 상태의 주인**

> "이 데이터는 누가 관리하는가?"

**③ 기능 간 연계**

> "A 기능의 결과가 B 기능에 어떻게 전달되는가?"

이 세 개가 정해지면 **Pinia는 사실상 "설계한 상태들을 코드로 옮기는 작업"**이 된다.

반대로 이걸 안 정하고 Pinia부터 만들면 `WeatherStore`, `ConfigStore`, `AuthStore`에 **아무 데이터나 집어넣게 돼서 나중에 다시 뜯어고칠 가능성이 높아.**
