# 디자인 설계 기준

> "무엇을 왜 만드는가"는 [service_architecture.md](./service_architecture.md), "어디에 구현하는가"는 [vue_architecture.md](./vue_architecture.md)가 답한다. 이 문서는 **정해진 정보를 화면에서 어떻게 보여줄 것인가**에만 답한다.

세 문서 역할 분담:

| 파일 | 답하는 질문 |
| --- | --- |
| [service_architecture.md](./service_architecture.md) | 무엇을, 왜 만드는가 |
| **design_architecture.md** (이 문서) | 어떻게 보여줄 것인가 |
| [vue_architecture.md](./vue_architecture.md) | 어디에 구현할 것인가 |

경계: "풍속 50m/s 이상이면 위험"(서비스) / "위험 카드는 좌측에 4px 강조 바를 갖는다"(이 문서) / "위험 목록은 computed로 만든다"(Vue).

---

## 1. 왜 지금 디자인 기준을 세우는가

디자인 기준 없이 화면을 다섯 번 만들면, 같은 의미의 값이 다섯 번 각각 결정된다. 실측 결과:

| 의미 | 흩어진 값 (개수) |
| --- | --- |
| 주 버튼 배경색 | `#3498db` / `#007bff` / `#4b6584` / `#2c3e50` (4종) |
| 카드 모서리 반경 | `12px` / `8px` / `6px` / `4px` / `16px` / `30px` (6종) |
| 위험/경고 빨강 | `#d91010` / `#ff7675` / `#c0392b` / `#e74c3c` / `#d63031` (5종) |
| 본문 폰트 | Inter / Pretendard / Noto Sans KR (3종, 전부 미배포) |

`.dashboard-wrapper { width: 600px }`가 4개 파일에 동일 복제, `.weather-card` 규칙이 전역 CSS와 컴포넌트 scoped style에 값까지 같게 이중 선언되어 있다.

> 디자인은 **정보의 우선순위를 시각 속성으로 번역하는 일**이다. 우선순위가 정해지지 않은 채 색부터 고르면 결과가 임의값이 된다.

증거: 가장 위험한 "폭우+강풍 동시 발생"에는 파란색(`#2ba9d2`, 안내색 계열)이, 가장 안전한 "산책하기 좋은 날씨"에는 주황색(`#ffa72cfa`, 경고색 계열)이 붙어 있다(`WeatherBadge.vue:27,33` + `exercise.css:122,132`). 색을 고르는 작업은 이뤄졌지만 번역할 우선순위 원본이 없어 결과가 뒤집혔다.

**지금이 적기인 이유**: 화면 종류(라우트 5개)와 공통 부품(7개 컴포넌트)이 이미 확정돼 "이 부품의 시각 규격"이라는 문장을 쓸 대상이 존재하고, 동시에 하드코딩 규모(CSS 약 300줄)가 전면 치환 가능한 수준이다.

이 문서가 규정하지 않는 것: 브랜드 아이덴티티·로고, 애니메이션 연출 문법, 어떤 데이터를 보여줄지(서비스 문서), 상태를 누가 소유할지(Vue 문서).

---

## 2. 정보 우선순위

### 도출 논리

```text
컨셉: 사용자의 행동 판단을 돕는다
  → 어떤 정보가 사용자의 행동을 바꾸는가?
  → 행동을 바꾸는 힘이 큰 순서로 줄을 세운다
```

> 정보 우선순위는 "무엇이 중요해 보이는가"가 아니라 **"이 정보를 못 보면 사용자가 잘못된 행동을 하는가"**로 결정한다.

### 단계 정의

정보 축(P0~P4)과 도구 축(T1~T2), 광고(X)를 분리한다 — 도구가 아무리 자주 쓰여도 위험 경보보다 눈에 띄면 안 되고, 부가 정보보다는 찾기 쉬워야 하기 때문이다.

| 단계 | 포함 정보 | 상태 |
| --- | --- | --- |
| **P0** 위험 경보 | 위험 판정 결과, 위험 지역 수, 대응 지시 | `[현재]` |
| **P1** 현재 상태 | 도시명, 날씨 상태, 현재 기온 | `[현재]` |
| **P2** 판단 근거 수치 | 습도, 풍속 | `[현재]` |
| **P3** 예보 | 시간별·일별 예보 | `[예정]` |
| **P4** 부가 정보 | 서비스 소개, 관측 시각 | `[현재]` |
| **T1** 주 탐색 | 전역 네비게이션, 도시 검색 | `[현재]` |
| **T2** 보조 탐색 | 상태 검색, 필터, 단위 토글 | `[현재]` |
| **X** 광고 | 외부 슬롯 | `[예정]` |

풍속(P2)이 `>=50`이 되는 순간 P0을 생성한다(2.2 승격 관계) — 그래서 P2 수치는 절대 완전히 숨기지 않는다.

### 우선순위 → 시각 속성 번역표

| 단계 | 크기 | 색·대비 | 강조 수단 |
| --- | --- | --- | --- |
| **P0** | 본문의 1.0~1.15배 | 위험 단계색 배경 채움 또는 좌측 바, 대비 4.5:1↑ | **배경 채움+아이콘+테두리**(유일하게 3중 허용) |
| **P1** | 화면 내 최대(기온) | 최고 대비, 배경 채움 금지 | 크기+굵기(2중까지) |
| **P2** | 본문보다 작게 | 약한 회색 | 없음(라벨만 약하게) |
| **P4** | 본문보다 작게 | 최저 대비(단, AA 기준은 지킴) | 없음 |
| **X** | 슬롯 고정 | 서비스 색 사용 금지 | "광고" 라벨 필수, 위험색 금지 |

### 파생 규칙

1. **배경 채움은 P0과 광고 라벨에만.** 현재 `.badge`는 P0과 P2(온도감)에 동일하게 배경 채움을 써 "❄️ 선선함"이 "🌊 폭우+강풍"과 같은 시각 강도를 갖는다.
2. **색만으로 우선순위를 표현하지 않는다.** 위험 단계는 아이콘/형태를 반드시 동반.
3. **강조 수단은 단계당 정해진 개수를 넘지 않는다.**
4. **상위 단계는 하위 단계보다 위에 온다**(예외: T1은 항상 최상단, P4는 항상 하단).
5. **P2 수치는 감추지 않는다.**

### 강조 총량 규칙

> 모든 것을 강조하면 아무것도 강조되지 않는다.

현재 목업 8개 중 4개(정확히 50%)가 위험 판정을 통과한다. 절반에 최고 강조를 주면 배경이 된다.

| 대상 | 상한 |
| --- | --- |
| 화면당 최강 강조(배경+최대크기+아이콘) | 1개 |
| 목록에서 P0 비율이 50%를 넘을 때 | 색을 늘리지 말고 **정렬**로 해결(위험 항목을 상단으로) |
| 카드당 배지 | 최대 2개, 그중 P0은 1개 |
| 화면당 위험 강조색 계열 | 1개 계열(현재 빨강만 5종 사용 중) |

### 진단 — 현재 무너진 우선순위

스크린샷·실측 결과:
1. 배지만 유일하게 배경이 채워져 있어 **P2 정보가 P1을 이긴다.**
2. 전역 CSS 리셋(`font-weight: normal`)이 `h4`까지 무효화해 도시명과 본문이 완전히 같은 굵기·크기로 렌더링된다.
3. 위험/안전 카드가 배경·테두리 동일 — 위험 색 배정 자체가 위험도와 역전돼 있다(제주=최고위험=파랑, 서울=무위험=주황).
4. 카드가 항상 채운 배지 2개(온도감+추천)를 가져 목록 전체가 색 덩어리로 보인다(배지 간 margin 0).
5. StatusBar가 필터 변경과 무관하게 낡은 선택 문구를 유지한다.

---

## 3. 화면 배치 기준

### 원칙

1. **첫 화면에는 P0과 P1만.** 현재 홈은 검색 카드 2개를 목록과 같은 무게로 세로로 쌓아 카드를 화면 밖으로 밀어낸다.
2. **탐색 도구는 결과 바로 위, 결과보다 작게.**
3. **위험 정보는 항상 같은 자리**(화면 최상단 배너 / 카드 최상단 좌측). 현재는 온도감 배지 뒤에 붙어 위험 배지 시작 위치가 카드마다 다르다.
4. **한 화면에 하나의 주 콘텐츠 영역만.**
5. **목록은 우선순위 순으로 정렬**(위험 항목 상단). 현재는 배열 순서 그대로라 안전 지역 4곳이 상단을 차지한다.
6. **폭은 콘텐츠가 정한다**(`max-width` + `width:100%`). 현재 600px 고정 4중복이 375px 뷰포트에서 307px 오버플로우를 만든다.

### 앱 셸

`App.vue`는 5개 `.app-container`를 세로로 쌓아 과제 1~5를 한 페이지에 동시 렌더링하고, `RouterView`가 2곳(`:36`, `:58`)에 있어 같은 화면이 항상 2번 마운트된다. 목표 배치:

```text
┌ 헤더: 서비스명 [대시보드│경보│소개] [단위 토글] ┐  T1/T2
│ 경보 요약 배너 (P0, 화면당 1개, 위험 0건이면 미표시) │
│ 주 콘텐츠 — <RouterView/> 1개                     │  P0~P3
│ StatusBar                                          │  P4
│ 푸터: 데이터 출처                                  │  P4
└──────────────────────────────────────────────────┘
```

`<RouterView/>`는 앱 셸에 1개만 둔다. `#app`의 2열 그리드(`main.css:24-35`, 스캐폴딩 기본값)는 제거한다.

### View별 배치 요지

- **WeatherHomeView**: 검색·필터를 한 줄로 압축해 최상단 → 결과 수 피드백 → 위험 카드 상단 고정 정렬.
- **WeatherDetailView**: 뒤로가기를 최상단(현재는 최하단) → 위험 경보(있을 때만) → 기온(화면 내 최대) → 습도/풍속(라벨:값 2열, 현재는 문장형이라 훑을 수 없음).
- **WeatherAlertView**: 판정 기준 문구를 제목 아래 P4로(화면 전체가 P0이므로) → 카드는 Home과 동일 규격.
- **WeatherAboutView**: 전체가 P4. 현재 홈 버튼(`#3498db` 배경 + 굵은 글씨)이 화면에서 가장 강한 요소인 것을 정정 — 부가 정보 화면의 장식이 경보의 위험 표시보다 강해서는 안 된다.
- **NotFoundView**: 배치는 적절하나 다른 화면과 다른 폰트·색·반경을 쓰고 있어 톤을 통일한다.

---

## 4. 컴포넌트 시각 규격

### 공통 원칙

| 원칙 | 이유 |
| --- | --- |
| 단일 정의처 | `.weather-card`가 전역 CSS와 scoped style에 이중 선언돼 우선순위 판단이 필요한 상태를 막는다 |
| 토큰 참조 | 리터럴 대신 5절 토큰 사용. 현재 CSS 변수 23개 중 소비처는 base.css 자신뿐 |
| 인라인 스타일 금지 | `WeatherList.vue:19`가 인라인으로 위험 빨강을 한 종류 더 만든 사례 방지 |
| 요소 선택자 금지 | 전역 `input { width: 90% }`가 모든 입력창에 의도치 않은 여백을 만든다 |

### WeatherCard (가장 중요 — 목록에 8회 반복돼 서비스 인상을 결정)

현재 문제: 도시명·기온·습도·풍속 4줄이 완전히 같은 크기(리셋 결함), 기온이 문장 안에 숨음, 배지가 가장 강한 요소, 상세 버튼이 절대배치로 도시명과 충돌.

목표 위계:

```text
┃ ⚠ 위험 배지(P0, 위험 시만, 카드 최상단 좌측 고정)
┃  도시명(굵게,1.2배)         상태
┃  기온(카드 내 최대, 1.8~2.2배)
┃  습도·풍속(라벨 약하게 / 값 본문)                [>]
```

좌측 위험 바(4px)를 카드 전체 높이로 — 배경 채움 대신(2.4 총량 규칙), 훑을 때 좌측 한 열만 봐도 위험 분포가 보인다. 카드 전체 클릭이 상세 이동, `position:absolute` 버튼 금지.

### WeatherBadge (위험 전달의 유일한 요소)

**배지를 A(온도감)/B(활동 추천) 두 성격으로 분리**하고, **A는 배지가 아니다**(배경 채움 없는 텍스트로 강등 — 새 정보 없이 P0과 같은 형태를 갖는 것을 막는다).

B의 7개 분기를 4단계로 재배정: safe → caution → warning → danger, **단조 증가**하는 채도·대비를 갖는다(현재 danger가 파랑이라 caution인 진빨강보다 약함). 같은 단계 안의 분기는 색이 아니라 문구로 구분. 텍스트는 배경 대비 4.5:1 이상(현재 6종 중 5종 미달, 최저 1.94).

배지 문구는 **라벨**이지 문장이 아니다(12자 이내, "🌪️ 강풍+고온"). 지시문("외출을 삼가세요")은 배지 밖 본문으로. **판정 임계값을 문구에 노출하지 않는다**("25도 이상" 같은 표기는 삭제 — 경보 화면 기준 안내에서 한 번만 설명).

### SearchBar / StatusBar / UnitToggler / 네비게이션 (요지)

- SearchBar: 폭 100%(현재 전역 `input:90%`가 여백을 만듦), 라벨을 `h3`로 마크업하지 않는다(도구가 정보 섹션과 같은 위계를 갖게 됨).
- StatusBar: 배경 채움·성공색(초록) 금지, P4 정보에 P0 수준 강도를 주지 않는다. 좌측 정렬(문구 길이가 매번 달라 중앙 정렬은 위치가 흔들림).
- UnitToggler: 세그먼트 형태로(현재는 버튼 하나로 다음 상태 예측 불가), 주 버튼 고유색 금지.
- 네비게이션: 화면당 1개만(현재 2벌), 구분자 문자 대신 여백, 활성 표시는 색+하단 바 2중.

---

## 5. 디자인 토큰

### 도입 이유

기존 하드코딩을 참조로 바꾸는 것이 첫 작업이다(신규 변수를 늘리는 것이 아니다 — CSS 변수 23개가 이미 있지만 아무도 안 쓴다).

### 네이밍 규칙

> 이름은 **색 이름이 아니라 역할 이름**으로 짓는다. `--color-red`가 아니라 `--color-danger`.

이유: 임계값 정정으로 위험 도시가 바뀌면 색만 재배정되면 되는데, `--color-red`였다면 "이제 red가 아니라 orange"라는 판단이 컴포넌트에 남는다.

### 색 토큰(기존 값에서 통합, 신규 최소화)

| 토큰 | 값 | 비고 |
| --- | --- | --- |
| `--color-surface` | `#ffffff` | 기본 표면 |
| `--color-surface-sunken` | `#f8f9fa` | 한 단계 들어간 패널. `#f1f2f6` 폐기(구분되지 않음) |
| `--color-border` | `#dee2e6` | `#e9ecef` 폐기(패널 위에서 명도차 거의 없음) |
| `--color-text` / `--color-text-muted` | `#2c3e50` / `#6c757d` | muted는 `#7f8c8d`(대비비 3.48, AA 미달) 대체 |
| `--color-danger` / `--color-warning` / `--color-safe` / `--color-info` | `#d91010` / `#b35c00`(신규) / `#2e7d32` / `#0056b3` | 4개 중 3개가 이미 코드 안에 있던 값 |

**날씨 상태 색과 위험도 색을 분리한다**: 위험도는 채운 배지(고채도)로만, 날씨 종류는 연한 칩(저채도, 6종 신규)으로만 표현한다. 같은 hue(파랑=비)를 겹치더라도 채도·형태로 구분해 위험 경고가 날씨 종류 표시로 오독되지 않게 한다.

### 타이포 / 간격 / 반경

- **선행 조건**: `base.css`의 전역 `font-weight: normal` 리셋이 heading 굵기를 전부 무효화한다. 이것부터 제거해야 크기 스케일이 의미를 갖는다.
- 크기: `xs 12 / sm 14 / base 16(15에서 상향, iOS 자동확대 방지) / md 18(카드제목,신규) / lg 20(섹션제목,신규) / xl 24 / 2xl 32`.
- 간격: **4배수 스케일**(8배수 아닌 이유 — 현재 실사용 값 18종 중 4배수가 이미 9종으로 다수. 8배수만 허용하면 12px·20px 실사용값을 표현 못 함).
- 반경: `sm 4 / md 8 / lg 12 / full 999`(6종→4종. 6px·8px는 눈으로 구분 안 됨).
- 그림자: 2종으로 축소(실측 결과 홈 라우트에 실제 쓰이는 것도 2종뿐).

### 다크 모드

> **지원하지 않는다.** 기존 `prefers-color-scheme: dark` 블록을 삭제한다.

현재 상태가 미지원보다 나쁘다 — 다크 강제 시 `body`만 어두워지고 카드는 흰색 그대로 남는다("지원하려다 만" 상태). 라이트 모드 대비비조차 5종이 AA 미달인 상태에서 두 벌을 만들면 둘 다 미달로 남는다. 재검토 조건: 토큰이 전 컴포넌트에 적용되고 대비비가 전부 통과한 뒤.

---

## 6. 상태 표현 기준

### 상태 목록과 규격

| 상태 | 보여주는 것 | 레이아웃 이동 방지 |
| --- | --- | --- |
| 초기 로딩 | 실제 카드와 같은 구조의 스켈레톤 3장 | 스켈레톤이 실카드와 같은 padding·radius |
| 갱신 중 | 기존 목록 유지 + 헤더 우측 진행 표시 | 기존 목록 유지 |
| 빈 결과 | 중립 아이콘 + 안내 + 검색 초기화 버튼 | 최소 높이 카드 3장분 |
| 오류 | 위험색 아이콘 + 원인 + 다시 시도 버튼 | 동일 |
| 권한 없음 | 안내색(info) + 로그인 유도 | 동일 |

### 빈 결과와 오류를 다르게 표현하는 이유

> 빈 결과는 서비스가 정상 동작한 결과이고, 오류는 실패한 상태다. 다음 행동이 정반대다.

현재 `WeatherList.vue:19`가 빈 결과 문구를 인라인 빨강으로 표시해, 정상 동작인데 사용자가 자기 잘못이라 느끼게 만든다.

### 로딩 표현

> 초기 로딩은 스켈레톤, 갱신 중은 인라인 진행 표시. 전면 스피너와 `alert()`는 쓰지 않는다.

스피너는 높이가 거의 없어, 스피너→카드 8장 전환 순간 아래 요소가 수백 px 밀린다.

### 위험 상태

> 색은 위험을 전달하는 세 가지 수단(색·아이콘·텍스트) 중 하나일 뿐이다. 항상 함께 쓴다.

안전 상태에는 채운 배지를 쓰지 않는다(현재 항상 하나가 표시돼 배지가 "특이사항 신호"로 기능하지 못함). 온도감은 배지 체계에서 빼고 보조 표기로.

### StatusBar의 위치

> StatusBar는 "직접 조작에 대한 일시적 피드백" 전용이다. 로딩·오류·빈 결과는 절대 StatusBar로 표현하지 않는다.

이유: StatusBar는 항상 초록으로 고정돼 있어("초록색 오류"가 됨), 필터 변경 후에도 낡은 문구를 유지한다(실측: "경주이 선택되었습니다"가 필터 후에도 남음).

---

## 7. 반응형 기준

### 현재 실태

실제 기능 화면에 미디어쿼리가 0건. 유일한 breakpoint(1024px, 스캐폴딩 기본값)는 콘텐츠 성격과 무관하게 전체를 2열로 쪼갠다. 실측: 1440px에서 데스크톱도 가로 스크롤 발생, 375px에서 307px 오버플로우.

### 원칙

> **모바일 우선.** 주 사용 맥락이 외출 직전 확인이므로 좁은 폭이 기본이다.

| 구간 | 폭 | 우선순위 |
| --- | --- | --- |
| mobile | ~767px | 1순위 |
| desktop | 1120px~ | 2순위 |
| tablet | 768~1119px | 3순위 |

(1024px을 breakpoint로 재사용하지 않는 이유: 그 값이 현재 문제의 원인이라 기존 규칙과 충돌할 수 있다.)

### 구간별 변화

카드 목록: 1열→2열, 네비게이션: 가로스크롤 한 줄→중앙 정렬, 검색: 세로 2단→가로 2단, 좌우 패딩: 16→24→32px.

### 폭 규정

> `width` 고정을 금지하고 `max-width` + `width:100%`만 사용한다.

최소 지원 폭 320px, 최대 콘텐츠 폭 640px(현재 600px 검증값 + 여유), 페이지 최대 폭 1120px. `.dashboard-wrapper` 4중복을 이 값으로 한 곳에 통합하면 실측 오버플로우 두 건이 모두 해소된다.

### 터치 대상

최소 44×44px(현재 상세 버튼 실측 70.1×32px로 세로 미달), 인접 간격 8px 이상. 카드 전체 클릭과 카드 내부 버튼을 겹치게 두지 않는다.

---

## 8. 접근성 기준과 광고 배치

### 접근성을 다루는 이유

> 위험 표시를 읽지 못하는 사용자가 있다면, 그 사용자에게 이 서비스는 작동하지 않는 것이다.

현재 배지 6종 중 5종이 WCAG AA(4.5:1) 미달(최저 1.94), 적록색각 이상에서 위험색과 강풍색이 회색으로 수렴, 정상 시력에서도 이미 위험도-색 배정이 역전돼 있다.

### 최소 준수 항목(WCAG 2.1 AA)

| 항목 | 기준 | 조치 |
| --- | --- | --- |
| 본문 대비 | 4.5:1↑ | muted 텍스트 `#6c757d` 교체 |
| 배지·버튼 텍스트 | 4.5:1↑ | 5.3 색 토큰으로 전면 교체 |
| **색 단독 의존 금지** | 색 외 수단 1개↑ | 위험 카드에 좌측 바+텍스트 라벨 추가(현재 카드는 위험 여부와 무관하게 동일한 흰 배경) |
| 키보드 접근 | Tab+Enter로 전 조작 가능 | 클릭 가능한 `div`(카드) 정정 |
| 포커스 표시 | 2px↑ 가시적 | `:focus-visible` 규칙 신설(현재 0건) |
| 폼 레이블 | 프로그램적 연결 | 검색 입력의 `h3` 라벨을 `<label for>`로 |
| 문서 언어 | `lang` 지정 | `index.html`의 빈 `lang=""` → `lang="ko"` |

### 현재 결함 요지

클릭 가능한 `div`(카드), 레이블 없는 입력, `<main>` 2개, 헤딩 단계 건너뛰기(h1→h3), `alert()`로 정보 전달(포커스 관리가 앱 통제를 벗어남), 포커스 스타일 부재, 터치 타깃 미달.

### 광고 배치 규격

정책(둘지 말지, 어디에)은 [service_architecture.md](./service_architecture.md) 10절이 정한다. 이 절은 정책이 확정됐을 때의 시각 규격만 정의한다.

> 광고는 위험 정보와 같은 시각 언어를 쓰지 않는다.

이유: 채운 배지는 이 서비스에서 "지금 행동을 바꿔라"를 독점한다. 광고가 같은 형태를 쓰면 (a) 광고를 경보로 오인하거나 (b) 반대로 경보를 광고로 오인해 무시하게 된다 — (b)가 훨씬 위험하다.

| 항목 | 규격 |
| --- | --- |
| 표기 | 좌상단 "광고" 라벨 |
| 배경/경계 | `surface-sunken` + `border`, 그림자 없음(광고가 콘텐츠보다 떠 보이면 안 됨) |
| 최대 높이 | mobile 120px / desktop 160px |
| 색·형태 | 위험도색·날씨tint·채운 배지 형태 전부 금지 |
| 배치 금지 구역 | 위험 경보 화면 전체, 카드 목록 중간, 검색-결과 사이, 빈 결과/오류 화면 |
| 로딩 실패 | 오류로 취급하지 않고 영역 자체를 제거(자리를 남기지 않음) |

---

## 요약 — 핵심 결정

| 결정 | 이유 |
| --- | --- |
| 정보를 P0~P4 + T1/T2 + X 축으로 분리 | 도구와 정보가 같은 축에 있으면 경쟁한다 |
| 화면당 최강 강조 1개, 위험 비율 50% 초과 시 정렬로 해결 | 절반이 강조되면 강조가 무의미해진다 |
| 위험도(채운 배지)와 날씨 종류(연한 칩)를 채도·형태로 분리 | 같은 hue를 쓰되 위험 경고가 날씨 표시로 오독되지 않게 |
| 다크 모드 미지원(기존 반쪽 구현 삭제) | "지원하려다 만" 상태가 미지원보다 나쁘다 |
| 모바일 우선, `max-width` 고정, 600px 고정폭 폐기 | 실측 오버플로우(모바일 307px, 데스크톱도 발생)를 해소 |
| 위험 경보 화면 광고 금지 | 판단을 돕는 서비스가 판단을 방해하는 모순 방지 |
| StatusBar는 조작 피드백 전용, 로딩/오류는 담지 않음 | 항상 초록 고정이라 "초록색 오류"가 된다 |

## [결정 필요] 목록

| 항목 |
| --- |
| 웹폰트를 실제로 배포할지, 시스템 폰트 스택으로만 갈지 |
| 광고 허용 후보(홈 하단 vs 상세 하단) 중 실제 채택 위치 — 서비스 문서 12절 결정 대기 |
| 단위 전환이 배지 판정에 영향을 주는지 — 서비스 문서 확정 대기(이 문서는 "판정 불변"을 제안) |
